# ============================================
# Sub2API NodeBB 论坛 Nginx 反代配置片段
# 将以下内容添加到主平台 /etc/nginx/sites-available/yourdomain.com
# 在 server { listen 443 ssl http2; ... } 块内添加
# ============================================

# WebSocket 升级映射(放在 http 块顶部)
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

# NodeBB 论坛反代(在 443 server 块内)
location /community/ {
    proxy_pass http://127.0.0.1:4567/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Prefix /community;
    proxy_read_timeout 60s;
    proxy_send_timeout 60s;

    # 安全头
    add_header Content-Security-Policy 
      "frame-ancestors 'self' https://yourdomain.com http://localhost:*" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}
